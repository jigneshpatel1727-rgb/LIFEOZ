const layers=[['01','Outer Skin','Flexible recycled-compatible elastomer'],['02','Energy Lattice','Cellular impact absorption'],['03','Thermal Layer','Heat spreading / buffering'],['04','Floating Core','Shock-isolated electronics'],['05','Sensor Strap','Health + motion sensing'],['06','Yansi Core','Intelligence + safety orchestration']];const list=document.querySelector('#layers');layers.forEach(([n,t,d])=>{const el=document.createElement('div');el.className='layer';el.innerHTML=`<b>${n} · ${t}</b>${d}`;list.appendChild(el)});const watch=document.querySelector('#watch');document.querySelector('[data-action="explode"]').onclick=()=>watch.classList.add('exploded');document.querySelector('[data-action="reset"]').onclick=()=>watch.classList.remove('exploded');document.querySelector('#mode').onclick=()=>{document.querySelector('#mode').textContent=document.querySelector('#mode').textContent==='ENERGY COCOON'?'YANSI INTELLIGENCE':'ENERGY COCOON'};const c=document.querySelector('#space'),x=c.getContext('2d');function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.scale(devicePixelRatio,devicePixelRatio)}resize();addEventListener('resize',resize);const stars=Array.from({length:130},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.3,a:.15+Math.random()*.65}));function draw(){x.clearRect(0,0,innerWidth,innerHeight);for(const s of stars){x.globalAlpha=s.a;x.fillStyle='#72eaff';x.beginPath();x.arc(s.x,s.y,s.r,0,Math.PI*2);x.fill()}requestAnimationFrame(draw)}draw();